import  * as areaInfo from './areaInfo';

export default class competition {
    area: areaInfo.default;
    code: string;
    id: number;
    lastUpdated:Date;
    name: string;
    plan: string;
  }